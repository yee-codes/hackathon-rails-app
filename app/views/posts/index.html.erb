<h1>Post listing</h1>
 
<%= link_to 'Create a new post', new_post_path %>
<%= stylesheet_link_tag "posts" %>

<table>
  <tr>
    <th>Title</th>
    <th>Content</th>
    <th></th>
  </tr>
 
  <% @posts.each do |post| %>
    <tr>
      <td><%= post.title %></td>
      <td><%= post.content %></td>
      <td><%= link_to 'Show', post_path(post) %></td>
    </tr>
  <% end %>
</table>